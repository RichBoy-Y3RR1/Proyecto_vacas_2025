Frontend Angular (skeleton)

- Desarrollo: `npm install` y `ng serve --open` (desde el proyecto Angular).
- En producción: `ng build --configuration production` y copiar `dist/` a `backend-java/src/main/webapp/browser/`.
- Configura `environment.ts` con la URL base del backend si usas proxies o CORS.
